// ASO Screenshot Test — Auto-generated by claude-appstore-generate-screenshots
// Review the navigation steps below before running.
// Run with: flutter drive \
//   --driver=test_driver/integration_test.dart \
//   --target=integration_test/aso_screenshots_test.dart \
//   --dart-define=TEST_EMAIL=$TEST_EMAIL \
//   --dart-define=TEST_PASSWORD=$TEST_PASSWORD \
//   -d <SIMULATOR_UDID>

import 'dart:io';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:integration_test/integration_test.dart';
import 'package:{{APP_PACKAGE}}/main.dart' as app;

void main() {
  final binding = IntegrationTestWidgetsFlutterBinding.ensureInitialized();

  // Screenshot output directory — relative to project root
  const screenshotDir = '{{SCREENSHOT_DIR}}';

  group('ASO Screenshots', () {
    setUpAll(() async {
      // Ensure output directory exists
      await Directory(screenshotDir).create(recursive: true);
    });

    testWidgets('Capture all screens', (tester) async {
      // Launch app
      app.main();
      await tester.pumpAndSettle(const Duration(seconds: 3));

      {{LOGIN_BLOCK}}

      {{SCREEN_STEPS}}
    });
  });
}

// ─── Helper Functions ────────────────────────────────────────────────────────

/// Take a screenshot and save it to the output directory.
/// [name] should be a slug like "01-home" or "03-search-results"
Future<void> takeScreenshot(
  IntegrationTestWidgetsFlutterBinding binding,
  WidgetTester tester,
  String name,
  String outputDir,
) async {
  await tester.pumpAndSettle(const Duration(milliseconds: 500));
  final bytes = await binding.takeScreenshot(name);
  final file = File('$outputDir/$name.png');
  await file.writeAsBytes(bytes);
  // ignore: avoid_print
  print('Screenshot saved: ${file.path}');
}

// ─── LOGIN BLOCK TEMPLATE ─────────────────────────────────────────────────────
// Replace {{LOGIN_BLOCK}} with this block if login is required:
//
// // --- Login ---
// final testEmail = const String.fromEnvironment('TEST_EMAIL');
// final testPassword = const String.fromEnvironment('TEST_PASSWORD');
//
// // Wait for login screen
// await tester.pumpAndSettle(const Duration(seconds: 2));
//
// // Check if already logged in (cached session)
// final isOnHomeScreen = find.byType(HomeScreen); // Replace with your home widget type
// if (!tester.any(isOnHomeScreen)) {
//   // Fill email field
//   await tester.enterText(find.byType(TextField).first, testEmail);
//   await tester.pumpAndSettle();
//
//   // Fill password field
//   await tester.enterText(find.byType(TextField).last, testPassword);
//   await tester.pumpAndSettle();
//
//   // Tap login button
//   await tester.tap(find.text('Sign In')); // Replace with your button text/key
//   await tester.pumpAndSettle(const Duration(seconds: 3));
// }

// ─── SCREEN STEPS TEMPLATE ───────────────────────────────────────────────────
// Replace {{SCREEN_STEPS}} with navigation + screenshot calls like:
//
// // --- Screen 1: Home ---
// // (already on home screen after launch/login)
// await takeScreenshot(binding, tester, '01-home', screenshotDir);
//
// // --- Screen 2: Search ---
// // Navigate to search tab
// await tester.tap(find.byIcon(Icons.search)); // or find.byKey(Key('search_tab'))
// await tester.pumpAndSettle(const Duration(seconds: 1));
// // Enter a search query to show results
// await tester.enterText(find.byType(TextField), 'coffee');
// await tester.pumpAndSettle(const Duration(seconds: 2));
// await takeScreenshot(binding, tester, '02-search', screenshotDir);
//
// // --- Screen 3: Profile ---
// await tester.tap(find.byIcon(Icons.person)); // or find.byKey(Key('profile_tab'))
// await tester.pumpAndSettle(const Duration(seconds: 1));
// await takeScreenshot(binding, tester, '03-profile', screenshotDir);
