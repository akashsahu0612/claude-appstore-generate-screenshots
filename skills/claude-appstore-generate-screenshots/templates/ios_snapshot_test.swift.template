// ASO Snapshot Test — Auto-generated by claude-appstore-generate-screenshots
// Review the navigation steps below before running.
// Run via fastlane snapshot or directly with:
//   xcodebuild test \
//     -scheme "{{APP_TARGET}}UITests" \
//     -destination "platform=iOS Simulator,name=iPhone 17 Pro" \
//     -testPlan AsoScreenshots

import XCTest

class AsoSnapshotTests: XCTestCase {

    var app: XCUIApplication!

    override func setUpWithError() throws {
        continueAfterFailure = false
        app = XCUIApplication()

        // Pass credentials via environment (set by fastlane snapshot or xcrun simctl)
        // Never hardcode credentials in test code
        setupSnapshot(app)
        app.launch()
    }

    func testCaptureAllScreens() throws {
        // Allow app to fully load
        sleep(2)

        {{LOGIN_BLOCK}}

        {{SCREEN_STEPS}}
    }

    // MARK: - Helpers

    /// Navigate to a screen and take a snapshot.
    /// [name] should be a slug like "01-Home" — fastlane snapshot uses this as the filename.
    func captureScreen(name: String) {
        // Allow animations to settle
        Thread.sleep(forTimeInterval: 0.5)
        snapshot(name)
    }
}

// ─── LOGIN BLOCK TEMPLATE ─────────────────────────────────────────────────────
// Replace {{LOGIN_BLOCK}} with this block if login is required:
//
// // --- Login ---
// let testEmail = ProcessInfo.processInfo.environment["TEST_EMAIL"] ?? ""
// let testPassword = ProcessInfo.processInfo.environment["TEST_PASSWORD"] ?? ""
//
// // Check if already on home screen (cached session)
// let homeTabBar = app.tabBars.firstMatch
// if !homeTabBar.waitForExistence(timeout: 3) {
//     // Fill email
//     let emailField = app.textFields["Email"] // Replace with your accessibility identifier
//     emailField.tap()
//     emailField.typeText(testEmail)
//
//     // Fill password
//     let passwordField = app.secureTextFields["Password"]
//     passwordField.tap()
//     passwordField.typeText(testPassword)
//
//     // Tap sign in
//     app.buttons["Sign In"].tap() // Replace with your button label or identifier
//     sleep(3)
// }

// ─── SCREEN STEPS TEMPLATE ───────────────────────────────────────────────────
// Replace {{SCREEN_STEPS}} with navigation + snapshot calls like:
//
// // --- Screen 1: Home ---
// // Already on home screen after launch/login
// captureScreen(name: "01-Home")
//
// // --- Screen 2: Search ---
// app.tabBars.buttons["Search"].tap() // Replace with your tab label
// sleep(1)
// let searchField = app.searchFields.firstMatch
// searchField.tap()
// searchField.typeText("coffee")
// sleep(2)
// captureScreen(name: "02-Search")
//
// // --- Screen 3: Profile ---
// app.tabBars.buttons["Profile"].tap()
// sleep(1)
// captureScreen(name: "03-Profile")
//
// // --- Screen 4: Detail View ---
// // Navigate back to list first
// app.navigationBars.buttons.firstMatch.tap()
// sleep(0.5)
// // Tap first item to open detail
// app.tables.cells.firstMatch.tap()
// sleep(1)
// captureScreen(name: "04-Detail")
